{
  "name": "<%= _.slugify(appname) %>",
  "description": "<%= pkg.description %>",
  "version": "0.0.0",
  "main": "source/scripts/boot.js",
  "devDependencies": {
    "browserify": "^10.1.0",
    "browserify-shim": "^3.8.6",
    "connect-livereload": "^0.5.3",
    "deamdify": "^0.1.1",
    "deglobalify": "^0.2.0",
    "grunt": "^0.4.5",
    "grunt-autoprefixer": "^2.2.0",
    "grunt-browserify": "^3.6.0",
    "grunt-concurrent": "^1.0.0",
    "grunt-contrib-clean": "^0.6.0",
    "grunt-contrib-copy": "^0.8.0",
    "grunt-contrib-cssmin": "^0.12.2",
    "grunt-contrib-imagemin": "^0.9.3",<% if (includeJade) { %>
    "grunt-contrib-jade": "^0.14.1",<% } %>
    "grunt-contrib-jshint": "^0.11.0",
    "grunt-contrib-uglify": "^0.8.1",
    "grunt-contrib-watch": "^0.6.1",
    "grunt-exec": "^0.4.6",
    "grunt-htmlcompressor": "^0.1.10",
    "grunt-newer": "^1.1.0",
    "grunt-processhtml": "^0.3.7",
    "grunt-sass": "^0.18.1",
    "grunt-svgmin": "^2.0.1",
    "imagemin-mozjpeg": "^4.0.0",
    "jshint-stylish": "^1.0.0",
    "load-grunt-config": "^0.16.0",
    "load-grunt-tasks": "^3.1.0",
    "time-grunt": "^1.1.0"
  },<% if (!includeBower) { %>
  "dependencies": {
    "icons": "github:FortAwesome/Font-Awesome/tarball/master#4.3.0",
    "purecss": "github:yahoo/pure/tarball/master#0.6.0"<% if (hasFeat) { %>,<% } if (includeModernizr) { %>
    "modernizr": "github:Modernizr/Modernizr/tarball/master#2.8.3"<% if (includejQuery || includeSizzle) {%>,<%}} else { if (includeHTML5Shiv) { %>
    "html5shiv": "github:aFarkas/html5shiv/tarball/master#3.7.2"<% if (includejQuery || includeSizzle) {%>,<%}}} if (includejQuery) { %>
    "jquery": "github:components/jquery/tarball/master#1.11.2"<% } else { if (includeSizzle) { %>
    "sizzle": "github:jquery/sizzle/tarball/master#2.0.0"<% }} %>
  },<% } %>
  "browser": {},
  "browserify-shim": {},
  "scaffold": {
    "server": "server",
    "static": "client",
    "source": "source",
    "tmp": ".tmp"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  }
}